[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "actionslab"
version = "1.0.0"
description = "ActionsLab — laboratório prático de CI/CD com GitHub Actions (Python + Pages)"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
authors = [{ name = "Guilherme Roesler" }]
keywords = ["ci-cd", "github-actions", "devops", "learning"]

[project.optional-dependencies]
dev = [
    "pytest==9.0.3",
    "pytest-cov==5.0.0",
    "ruff==0.7.4",
    "pip-audit==2.7.3",
]

[tool.setuptools]
py-modules = ["calculadora"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"

[tool.coverage.run]
source = ["calculadora"]
branch = true

[tool.coverage.report]
fail_under = 80
show_missing = true

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
